# RSA Common Modulus

## 题目信息

- 分类：Crypto
- 难度：Easy
- 关键字：RSA、同模攻击、扩展欧几里得

## 原理

当两个密文使用相同模数 `n`，但指数 `e1` 与 `e2` 互素时，可以通过扩展欧几里得找到 `s1`、`s2`，使得：

```text
s1 * e1 + s2 * e2 = 1
```

于是明文满足：

```text
m = c1^s1 * c2^s2 mod n
```

## 结论

这类题目重点是处理负指数，需要用模逆转换。
